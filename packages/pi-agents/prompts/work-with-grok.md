---
description: Delegate a task to the general agent on the Grok ACP runtime (structured tool calls + usage)
---

Use the `general` agent to complete the following task:

$@

Per-call overrides for the `general` agent:

- `runtime`: `grok-acp`
- `model`: `grok-4.5`
- `thinking`: `high`
